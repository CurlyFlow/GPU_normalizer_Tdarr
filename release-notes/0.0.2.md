## Performance

Bug: not same as CPU normalizer. Measured against Tdarr's legacy CPU-only `Normalize Audio` plugin, pre-stable `0.0.2` was faster on the 12s smoke but failed decoded-audio acceptance.

| Case | Legacy CPU `Normalize Audio` | GPU Normalize Audio `0.0.2` | Speed vs CPU | Decoded audio vs CPU output |
| --- | ---: | ---: | ---: | --- |
| 12s | `4.111s` | `2.603s` | `1.579x` | Failed: `energy_similarity=0`, SNR `-5.754 dB`, loudnorm deltas `input_i=+6.35`, `input_lra=-9.10`, `input_tp=+5.44`. |

This release is not performance-accepted because faster output below the CPU-normalizer audio target is a failed result.
