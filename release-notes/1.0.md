## Performance

`1.0` is the stable folder for the first CPU-normalizer-matching line. It keeps the `0.0.7` correctness target and adds relative runtime path resolution so the plugin no longer depends on hard-coded Tdarr install paths.

| Case | Legacy CPU `Normalize Audio` | GPU Normalize Audio `1.0` | Speed vs CPU | Decoded audio vs CPU output |
| --- | ---: | ---: | ---: | --- |
| 12s | measured | measured | slower | Expected from `0.0.7`: `energy_similarity=1`, `rms_diff=0`, `peak_diff=0`, `equal_fraction=1`. |
| 60s | measured | measured | slower | Expected from `0.0.7`: `energy_similarity=1`, `rms_diff=0`, `peak_diff=0`, `equal_fraction=1`. |
| 60min | `978.3s` | `8425.913s` | `0.116x` | Expected from `0.0.7`: `energy_similarity=1`, `rms_diff=0`, `peak_diff=0`, `equal_fraction=1`. |

This stable line is correctness-first: decoded parity is the reason for `1.0`; speed optimization remains pending.
