## Performance

Bug: not same as CPU normalizer. Pre-stable baseline release. No accepted performance matrix against Tdarr's legacy CPU-only `Normalize Audio` plugin was recorded for this version, so do not claim a speedup for `0.0.0`.

| Case | Legacy CPU `Normalize Audio` | GPU Normalize Audio `0.0.0` | Result |
| --- | ---: | ---: | --- |
| CPU-normalizer acceptance matrix | not recorded | not recorded | Bug: not same as CPU normalizer; baseline/rollback only. |
