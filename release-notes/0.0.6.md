## Performance

Bug: not same as CPU normalizer. Pre-stable `0.0.6` changed the default planner to `gpuSourcePort`, but no separate accepted performance matrix against Tdarr's legacy CPU-only `Normalize Audio` plugin was recorded before tagging.

Nearest recorded predecessor stats for the same GPU source-port line came from `0.0.5`:

| Case | Legacy CPU `Normalize Audio` | GPU source-port line | Speed vs CPU | Decoded audio vs CPU output |
| --- | ---: | ---: | ---: | --- |
| 60s | `17.794s` | `6.820s` | `2.609x` | Failed: `energy_similarity=0.597579`, SNR `7.906 dB`. |

Treat `0.0.6` as not performance-accepted. Compare against CPU `Normalize Audio` before production use.
